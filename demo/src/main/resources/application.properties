# Connect to the database on the port exposed by Docker, using the correct user and password
spring.datasource.url=jdbc:mysql://localhost:3307/sms?useSSL=false&serverTimezone=UTC
spring.datasource.username=user
spring.datasource.password=password
spring.jpa.hibernate.ddl-auto=update
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect


# ========== Server Configuration ==========
server.port=8080
spring.main.allow-bean-definition-overriding=true

# ========== JWT Configuration ==========
jwt.secret=your-super-secret-key
jwt.expiration=86400000

# ========== Email Configuration (Mailtrap) ==========
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=6545f3dc440046
spring.mail.password=d9b5525f7d700b
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Make sure to use forward slashes "/" and that the path is correct for your machine.
file.upload-dir=C:/Users/Hi/Downloads/ecommerce basic/uploads

# ========== File Upload Configuration ==========
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=100MB

# AWS S3 Configuration - This correctly reads from the .env file
aws.accessKeyId=${AWS_ACCESS_KEY_ID}
aws.secretKey=${AWS_SECRET_ACCESS_KEY}
aws.region=${AWS_REGION}
aws.s3.bucketName=${AWS_S3_BUCKET_NAME}

# Enable Hibernate statistics for performance monitoring
spring.jpa.properties.hibernate.generate_statistics=true
logging.level.org.hibernate.stat=DEBUG